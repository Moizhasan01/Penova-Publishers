RewriteEngine On

# Let Apache serve existing files such as JavaScript, CSS, images, and fonts.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send client-side routes to the SPA entry point.
RewriteRule ^ index.html [L]